<article id="productDetail">
  <img id="productImage" src="{{product.image}}" alt="{{product.title}}" />
  <div id="productInfo">
    <h2 id="productTitle">{{product.title}}</h2>
    <p id="productPrice">💰 Price: <span>${{product.price}}</span></p>
    <p id="productStock">📦 Stock: <span>{{product.stock}}</span></p>
    <p id="productCategory">🏷️ Category: <span>{{product.category}}</span></p>
    <button id="addToCartBtn">Add to cart</button>
  </div>
</article>
